{%- comment -%} 
  Sale banners snippet
  Place this snippet in collection pages where you want clickable sale-range banners to appear.
  It links to tag-filtered collection pages: /collections/{{ collection.handle }}/sale-0-25 etc. 
{%- endcomment -%}

<section class="sale-range-banners" aria-label="Sale filters">
  <div class="sale-range-banners__inner container">
    <a
      href="{{ collection.url }}/sale-0-25"
      class="sale-banner"
      data-range="0-25"
      aria-label="Up to 25% off">
      <div
        class="sale-banner__image"
        role="img"
        aria-hidden="true">
          <!-- Use real images by replacing src below or use background-image via CSS -->
        <img src="{{ 'sale-25.png' | asset_url }}" alt="Up to 25% off" />
      </div>
      <div class="sale-banner__label">Up to 25% off</div>
    </a>

    <a
      href="{{ collection.url }}/sale-26-35"
      class="sale-banner"
      data-range="26-35"
      aria-label="Up to 35% off">
      <div
        class="sale-banner__image"
        role="img"
        aria-hidden="true">
        <img src="{{ 'sale-35.png' | asset_url }}" alt="Up to 35% off" />
      </div>
      <div class="sale-banner__label">26% – 35% off</div>
    </a>

    <a
      href="{{ collection.url }}/sale-36-50"
      class="sale-banner"
      data-range="36-50"
      aria-label="Up to 50% off">
      <div
        class="sale-banner__image"
        role="img"
        aria-hidden="true">
        <img src="{{ 'sale-50.png' | asset_url }}" alt="Up to 50% off" />
      </div>
      <div class="sale-banner__label">36% – 50% off</div>
    </a>
  </div>
</section>

{%- comment -%} 
  Notes: 
  - Replace 'sale-25.png', 'sale-35.png', 'sale-50.png' with actual image assets in /assets. 
  - This snippet only links to collection tag pages. It does not change products or tags. 
{%- endcomment -%}